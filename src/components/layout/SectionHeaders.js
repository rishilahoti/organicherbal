export default function SectionHeaders({ subHeader, mainHeader }) {
	return (
		<>
			<h3 className="uppercase py-4 md:text-6xl text-4xl text-primary font-semibold leading-4">
				{subHeader}
			</h3>
		</>
	);
}
